# Crosssight backend — see backend/README.md
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27           # feed polling (LAQN/DEFRA/Met Office) — B3
sse-starlette>=2.1    # SSE for /stream and /agent/stream
anthropic>=0.40       # agent layer — B4/B5 (import is lazy; app boots without it)
